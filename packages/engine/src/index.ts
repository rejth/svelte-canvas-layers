// Stub public API barrel for @canvas/engine.
// Real engine exports (Renderer, LayerManager, Camera, GeometryManager, etc.)
// arrive in Phase 2 when the core is extracted into this package.
export const ENGINE_PACKAGE_NAME = '@canvas/engine'
